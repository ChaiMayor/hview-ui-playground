import { toRefs as u, reactive as p, h as t } from "vue";
const d = {
  props: {
    data: {
      type: Object,
      required: !0
    },
    renderContent: Function,
    parentData: Array
  },
  setup(r) {
    const { data: a, renderContent: e, parentData: n } = u(r), o = p({
      data: a.value,
      parentData: n.value
    });
    return () => [e.value ? e.value(t, o) : t("span", a.value.label)];
  }
};
export {
  d as default
};
