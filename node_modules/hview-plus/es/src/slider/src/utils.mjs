const c = (u, t, n) => {
  if (u > n || u < t)
    throw new Error("\u4E0D\u80FD\u8D85\u51FA\u9650\u5B9A\u8303\u56F4(\u9ED8\u8BA40~100)");
  return !0;
}, i = (u, t, n) => {
  if (!u)
    return [];
  const o = Math.floor(n / t), s = u / o, e = [];
  for (let r = 1; r <= o; r++)
    e.push(s * r);
  return e;
}, d = (u, t, n) => {
  const o = (n - t) / 2;
  return u > t + o ? "end" : "start";
};
export {
  i as cutChunk,
  c as isOutBounds,
  d as judgeLocation
};
